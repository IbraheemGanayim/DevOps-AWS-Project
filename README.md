# DevOps-AWS-Project
Object detection service that consists of multiple containerized microservices that allows users to send images through an interactive Telegram bot, the service detects objects in the image and send the results to the user.
